<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tcc.appmarlycordeiro">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_itherapeutic"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_itherapeutic_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDvb2O_IdJdiJAvevC3P0-k5IHWjq0rxe4"/>

        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.PacientesActivity" />
        <activity android:name=".activity.CadastrarPacientesActivity" />
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.ConsultasActivity" />
        <activity android:name=".activity.EventosActivity" />
        <activity android:name=".activity.CadastrarConsultasActivity" />
        <activity android:name=".activity.CadastrarEventosActivity" />

    </application>

</manifest>